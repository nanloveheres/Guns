<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.stylefeng.guns.modular.system.dao.GradeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.stylefeng.guns.modular.system.model.Grade">
        <id column="grade_id" property="gradeId" />
        <result column="grade_classroomName" property="gradeClassroomname" />
        <result column="grade_num" property="gradeNum" />
        <result column="grade_seasonClass" property="gradeSeasonclass" />
        <result column="grade_level" property="gradeLevel" />
        <result column="deleteFlag" property="deleteFlag" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        grade_id, grade_classroomName, grade_num, grade_seasonClass, grade_level, deleteFlag
    </sql>

	<select id="insertCheck" resultType="java.lang.Integer">
	   select count(*) from sys_grade where grade_classroomName = #{gradeClassroomname}
	   and grade_seasonClass=#{gradeSeasonclass} and grade_level = #{gradeLevel} and IFNULL(deleteFlag,0) != -1 
	</select>

</mapper>
